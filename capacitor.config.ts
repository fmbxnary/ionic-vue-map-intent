import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-map-intent',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
